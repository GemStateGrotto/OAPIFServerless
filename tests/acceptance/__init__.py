"""Acceptance tests — run against a live deployed endpoint.

These tests are marked ``@pytest.mark.acceptance`` and excluded from normal
``unit``/``integration`` runs.  Run them on demand:

    pytest -m acceptance
"""
